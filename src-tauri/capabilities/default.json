{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力",
  "windows": ["main", "overlay", "sender", "sign", "login"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
